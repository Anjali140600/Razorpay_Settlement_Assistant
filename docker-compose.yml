services:
  app:
    build: .
    ports:
      - "8501:8501"
    command: streamlit run apps/streamlit_app.py --server.port=8501 --server.address=0.0.0.0
    volumes:
      - .:/app
    working_dir: /app
